# MongoDB Atlas connection string (use either MONGODB_URI or MONGO_URI)
# Replace <password> with your Atlas database user password
MONGODB_URI=mongodb+srv://github_db_user:<password>@cluster0.ftzo0tv.mongodb.net/yallastarter?retryWrites=true&w=majority
# MONGO_URI=... (alternative; MONGODB_URI takes precedence)

# JWT Secret — use a strong random string in production
# Generate one with: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_here

# Admin user password (required in production for admin seed; no default in production)
ADMIN_PASSWORD=YallaAdmin2025!

# Google OAuth (optional — Google login disabled if not set)
# For production: set Authorized redirect URIs in Google Cloud Console to:
#   https://YOUR_RENDER_URL/api/auth/google/callback
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Base URL for OAuth callback (required for Google login on Render)
# Production: https://yallastarter-pro.onrender.com  |  Local: http://localhost:3000
BASE_URL=https://yallastarter-pro.onrender.com

# Stripe Payments (required for coin purchases)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Client URL (for redirects and CORS)
# Production: https://yallastarter-pro.onrender.com  |  Local: http://localhost:3000
CLIENT_URL=https://yallastarter-pro.onrender.com

# Environment
NODE_ENV=development

# Port (Render sets PORT automatically; local default 3000)
PORT=3000
