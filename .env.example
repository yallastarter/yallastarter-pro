# MongoDB Atlas connection string
# Replace <password> with your Atlas database user password
MONGO_URI=mongodb+srv://github_db_user:<password>@cluster0.ftzo0tv.mongodb.net/yallastarter?retryWrites=true&w=majority

# JWT Secret — use a strong random string in production
# Generate one with: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_here

# Admin user password (set on first startup seed)
ADMIN_PASSWORD=YallaAdmin2025!

# Google OAuth (optional — Google login disabled if not set)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Stripe Payments (required for coin purchases)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Client URL (for redirects and CORS)
# Production: https://yallastarter.onrender.com
# Local: http://localhost:3000
CLIENT_URL=http://localhost:3000

# Environment
NODE_ENV=development

# Port (Render sets this automatically, no need to set in Render)
PORT=3000
